import { ToggleFilmCardDirective } from './toggle-film-card.directive';

describe('ToggleFilmCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleFilmCardDirective();
    expect(directive).toBeTruthy();
  });
});
